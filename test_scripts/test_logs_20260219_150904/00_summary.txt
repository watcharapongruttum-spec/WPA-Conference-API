WPA Conference API — Full System Test
Run at: Thu Feb 19 15:09:04 +07 2026
Base URL: http://localhost:3000
============================================


════ 1. AUTHENTICATION ════

  ── 1.1 Login ──
  PASS: Login User A (HTTP 200)
  PASS: Login User B (HTTP 200)

  ── 1.2 Login — invalid credentials ──
  PASS: Login with wrong password → 401 (HTTP 401)

  ── 1.3 Login — missing fields ──
  PASS: Login with empty fields → 422 (HTTP 422)

  ── 1.4 Change Password (A) ──
  PASS: Change password success → 200 (HTTP 200)
  PASS: Login with new password → 200 (HTTP 200)
  PASS: Revert password → 200 (HTTP 200)

  ── 1.5 Change Password — wrong current ──
  PASS: Change password wrong current → 401 (HTTP 401)

  ── 1.6 Change Password — mismatch confirmation ──
  PASS: Change password mismatch → 422 (HTTP 422)

  ── 1.7 Forgot Password ──
  PASS: Forgot password (valid email) → 200 (HTTP 200)
  PASS: Forgot password (nonexistent email) → 200 (HTTP 200)

  ── 1.8 Access without token → 401 ──
  PASS: Access profile without token → 401 (HTTP 401)

════ 2. PROFILE ════

  ── 2.1 Get own profile ──
  PASS: GET /profile (own) → 200 (HTTP 200)

  ── 2.2 Get profile by ID ──
  PASS: GET /profile/:id → 200 (HTTP 200)

  ── 2.3 Get profile — nonexistent ID ──
  PASS: GET /profile/99999999 → 404 (HTTP 404)

  ── 2.4 Update profile ──
  PASS: PATCH /profile → 200 (HTTP 200)

  ── 2.5 /delegates profile endpoint ──
  PASS: GET /delegates/profile → 200 (HTTP 200)

════ 3. DELEGATES ════

  ── 3.1 List delegates ──
  PASS: GET /delegates → 200 (HTTP 200)

  ── 3.2 Get delegate by ID ──
  PASS: GET /delegates/:id → 200 (HTTP 200)

  ── 3.3 Get own ID via delegates → 422 ──
  PASS: GET /delegates/:own_id → 422 (HTTP 422)

  ── 3.4 Delegates — nonexistent ──
  PASS: GET /delegates/99999999 → 404 (HTTP 404)

  ── 3.5 Search delegates — keyword ──
  PASS: GET /delegates/search?keyword=a → 200 (HTTP 200)

  ── 3.6 Search delegates — empty keyword ──
  PASS: GET /delegates/search (empty) → 200 (HTTP 200)

  ── 3.7 Search delegates — pagination ──
  PASS: GET /delegates/search (page 2) → 200 (HTTP 200)

  ── 3.8 QR Code ──
  PASS: GET /delegates/:id/qr_code → 200 (HTTP 200)

════ 4. DASHBOARD ════

  ── 4.1 Get dashboard ──
  PASS: GET /dashboard → 200 (HTTP 200)
  PASS: GET /dashboard (user B) → 200 (HTTP 200)

════ 5. SCHEDULES ════

  ── 5.1 List schedules ──
  PASS: GET /schedules → 200 (HTTP 200)

  ── 5.2 My schedule ──
  PASS: GET /schedules/my_schedule → 200 (HTTP 200)

  ── 5.3 My schedule with date filter ──
  PASS: GET /schedules/my_schedule?year=2024 → 200 (HTTP 200)

  ── 5.4 Schedule others ──
  PASS: GET /schedules/schedule_others?delegate_id=238 → 404 (200 or 404 ok)

  ── 5.5 Schedule others — missing delegate_id ──
  PASS: schedule_others missing delegate_id → 404 (error response ok)

════ 6. TABLES ════

  ── 6.1 Grid view ──
  PASS: GET /tables/grid_view → 200 (HTTP 200)

  ── 6.2 Time view ──
  PASS: GET /tables/time_view → 200 (HTTP 200)

  ── 6.3 Time view with filters ──
  PASS: GET /tables/time_view?date=today → 200 (HTTP 200)

  ── 6.4 Table show ──
  PASS: GET /tables/1 → 200 (HTTP 200)

════ 7. NETWORKING ════

  ── 7.1 Directory ──
  PASS: GET /networking/directory → 200 (HTTP 200)

  ── 7.2 Directory — pagination ──
  PASS: GET /networking/directory?page=2 → 200 (HTTP 200)

  ── 7.3 My connections ──
  PASS: GET /networking/my_connections → 200 (HTTP 200)

  ── 7.4 Pending requests ──
  PASS: GET /networking/pending_requests → 200 (HTTP 200)

════ 8. CONNECTION REQUESTS ════

  ── 8.1 Cleanup state ──

  ── 8.2 List all requests ──
  PASS: GET /requests → 200 (HTTP 200)

  ── 8.3 My received requests ──
  PASS: GET /requests/my_received (B) → 200 (HTTP 200)

  ── 8.4 Send friend request A→B ──
  PASS: POST /requests (A→B) → 201 (HTTP 201)

  ── 8.5 Send request to self → 422 ──
  PASS: POST /requests to self → 422 (HTTP 422)

  ── 8.6 Duplicate request → 422 ──
  PASS: POST /requests duplicate → 422 (HTTP 422)

  ── 8.7 Accept request (B accepts) ──
  PASS: PATCH /requests/:id/accept → 200 (HTTP 200)

  ── 8.8 Verify connection created ──
  PASS: Connection confirmed: A and B are friends

  ── 8.9 Unfriend A→B ──
  PASS: DELETE /networking/unfriend/:id → 200 (HTTP 200)

  ── 8.10 Unfriend again → 404 ──
  PASS: DELETE /networking/unfriend (duplicate) → 404 (HTTP 404)

  ── 8.11 Reject flow: A→B request, B rejects ──
  PASS: PATCH /requests/:id/reject → 200 (HTTP 200)

  ── 8.12 Request nonexistent target → 404 ──
  PASS: POST /requests nonexistent target → 404 (HTTP 404)

  ── 8.13 Re-establish friendship for downstream tests ──
  PASS: Re-establish friendship → 200 (HTTP 200)

════ 9. MESSAGES ════

  ── 9.1 Send message A→B ──
  PASS: POST /messages (A→B) → 201 (HTTP 201)

  ── 9.2 Send message B→A ──
  PASS: POST /messages (B→A) → 201 (HTTP 201)

  ── 9.3 Send message — blank content → 422 ──
  PASS: POST /messages blank content → 422 (HTTP 422)

  ── 9.4 Send message — missing recipient → 422 ──
  PASS: POST /messages no recipient → 422 (HTTP 422)

  ── 9.5 Send message — over 2000 chars → 422 ──
  PASS: POST /messages over 2000 chars → 422 (HTTP 422)

  ── 9.6 List messages ──
  PASS: GET /messages → 200 (HTTP 200)

  ── 9.7 Conversation A↔B ──
  PASS: GET /messages/conversation/:id → 200 (HTTP 200)

  ── 9.8 Conversation — pagination ──
  PASS: GET /messages/conversation (paged) → 200 (HTTP 200)

  ── 9.9 Rooms list ──
  PASS: GET /messages/rooms → 200 (HTTP 200)

  ── 9.10 Unread count ──
  PASS: GET /messages/unread_count → 200 (HTTP 200)

  ── 9.11 Unread count — invalid sender_id ──
  PASS: GET /messages/unread_count invalid id → 200 (returns 0) (HTTP 200)

  ── 9.12 Online status ──
  PASS: GET /messages/online_status → 200 (HTTP 200)

  ── 9.13 Update message (edit) ──
  PASS: PATCH /messages/:id (own) → 200 (HTTP 200)

  ── 9.14 Update message — not owner → 403 ──
  PASS: PATCH /messages/:id (not owner) → 403 (HTTP 403)

  ── 9.15 Read all messages ──
  PASS: PATCH /messages/read_all → 200 (HTTP 200)

  ── 9.16 Delete message ──
  PASS: DELETE /messages/:id → 200 (HTTP 200)

  ── 9.17 Delete already-deleted → 422 ──
  PASS: DELETE /messages (already deleted) → 422 (HTTP 422)

════ 10. CHAT ROOMS ════

  ── 10.1 List chat rooms ──
  PASS: GET /chat_rooms → 200 (HTTP 200)

  ── 10.2 Create group room ──
  PASS: POST /chat_rooms (group) → 201 (HTTP 201)

  ── 10.3 Create room — missing params → 400 ──
  PASS: POST /chat_rooms missing params → 400 (HTTP 400)

  ── 10.4 Join room (B joins) ──
  PASS: POST /chat_rooms/:id/join (B) → 200 (HTTP 200)

  ── 10.5 Join room again (idempotent) ──
  PASS: POST /chat_rooms/:id/join (again) → 200 (HTTP 200)

  ── 10.6 Leave room (B leaves) ──
  PASS: DELETE /chat_rooms/:id/leave (B) → 200 (HTTP 200)

  ── 10.7 Leave room (not member) → 404 ──
  PASS: DELETE /chat_rooms/:id/leave (not member) → 404 (HTTP 404)

  ── 10.8 Delete room (A = admin) ──
  PASS: DELETE /chat_rooms/:id (admin) → 200 (HTTP 200)

  ── 10.9 Delete room — nonexistent → 404 ──
  PASS: DELETE /chat_rooms/99999999 → 404 (HTTP 404)

════ 11. NOTIFICATIONS ════

  ── 11.1 List all notifications ──
  PASS: GET /notifications → 200 (HTTP 200)

  ── 11.2 Filter — system notifications ──
  PASS: GET /notifications?type=system → 200 (HTTP 200)

  ── 11.3 Filter — message notifications ──
  PASS: GET /notifications?type=message → 200 (HTTP 200)

  ── 11.4 Unread count ──
  PASS: GET /notifications/unread_count → 200 (HTTP 200)

  ── 11.5 Unread count — by type ──
  PASS: GET /notifications/unread_count?type=system → 200 (HTTP 200)

  ── 11.6 Mark single notification as read ──
  PASS: PATCH /notifications/:id/mark_as_read → 200 (HTTP 200)

  ── 11.7 Mark single (already read) → 200 ──
  PASS: PATCH /notifications/:id/mark_as_read (already read) → 200 (HTTP 200)

  ── 11.8 Mark single — not found → 404 ──
  PASS: PATCH /notifications/99999999/mark_as_read → 404 (HTTP 404)

  ── 11.9 Mark all as read ──
  PASS: PATCH /notifications/mark_all_as_read → 200 (HTTP 200)

  ── 11.10 Mark all as read — by type ──
  PASS: PATCH /notifications/mark_all_as_read?type=message → 200 (HTTP 200)

════ 12. LEAVE FORMS & TYPES ════

  ── 12.1 List leave types ──
  PASS: GET /leave_types → 200 (HTTP 200)

  ── 12.2 Get single leave type ──
  PASS: GET /leave_types/:id → 200 (HTTP 200)

  ── 12.3 Create leave form ──
  PASS: POST /leave_forms → 200

════ 13. DEVICE TOKEN ════

  ── 13.1 Update device token ──
  PASS: PATCH /device_token → 200 (HTTP 200)

  ── 13.2 Update device token — same value (idempotent) ──
  PASS: PATCH /device_token (same) → 200 (HTTP 200)

  ── 13.3 Update device token — missing → 422 ──
  PASS: PATCH /device_token (missing) → 422 (HTTP 422)

════ 14. SECURITY & EDGE CASES ════

  ── 14.1 Invalid JWT token ──
  PASS: Invalid token → 401 (HTTP 401)

  ── 14.2 Malformed Authorization header ──
  PASS: Malformed auth header → 401 (HTTP 401)

  ── 14.3 Access other user's notification → 404 ──
  PASS: Mark other user's notification → 404 (HTTP 404)

  ── 14.4 Unfriend nonexistent user → 404 ──
  PASS: Unfriend nonexistent user → 404 (HTTP 404)

  ── 14.5 QR code nonexistent → 404 ──
  PASS: QR code nonexistent → 404 (HTTP 404)

════ 15. END-TO-END USER JOURNEY ════

════ 15. E2E USER JOURNEY ════

  ── 15.1 Full connection flow: A→B→unfriend→reconnect ──
  PASS: E2E: A sends friend request (id=23)
  PASS: E2E: B accepts
  PASS: E2E: A sends message
  PASS: E2E: B replies
  PASS: E2E: A reads conversation
  PASS: E2E: A checks dashboard
  PASS: E2E: A checks notifications
  PASS: E2E: A unfriends B
  PASS: E2E: Confirmed unfriended

============================================
FINAL RESULTS
============================================
Total   : 108
Passed  : 108
Failed  : 0
Skipped : 0
Time    : 7s
Finished: Thu Feb 19 15:09:11 +07 2026

ALL TESTS PASSED
